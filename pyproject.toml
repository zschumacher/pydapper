[project]
name = "pydapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dsnparse>=0.2.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
