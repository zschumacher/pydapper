BEGIN
EXECUTE IMMEDIATE 'CREATE TABLE pydapper.owner (id NUMBER GENERATED BY DEFAULT AS IDENTITY,name varchar(255) NOT NULL)';
EXCEPTION
    WHEN OTHERS THEN IF SQLCODE = -955 THEN NULL; ELSE RAISE; END IF;
END;
